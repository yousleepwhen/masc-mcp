# OCaml phase-count baseline — transient grandfather list for the
# audit-ocaml-phase-count.sh validator.  Each non-comment line:
# "<repo-relative-path>:<lineno>".
#
# STATUS: empty.  iter 55 R-H-1.f (#14891) introduced the validator with
# four grandfathered "12-state" sites pending fix via iter 54 R-H-1.e
# (#14886).  That PR merged (all four now say "13-state"), so iter 65
# R-H-1.g drained the four lines — the CI step now enforces zero drift.
#
# Keep this file: the CI step still passes `--baseline <this file>`.  If a
# future change introduces a phase-count mention that cannot be fixed in
# the same PR, add its "<path>:<lineno>" here, reference the fix-PR in
# this comment, and remove it once that PR merges.
