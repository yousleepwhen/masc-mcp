# KTA R-16 — KeeperTaskAcquisition.tla: first-entry audit — model accurate; the spec preamble didn't point at the OCaml side's annotated correspondence (`run_keeper_cycle`'s "Spec navigation" block + `keeper_keepalive_signal.ml`'s `post_submit_task` `[@@fsm_guard]`), and the two producer paths (backlog vs operator `claim:` directive) weren't disclosed — sub-class 2

**Date**: 2026-05-12 · **Iteration**: 91 (`/loop` FSM/TLA+/OCaml drift hunt) · **Phase**: R (first-entry audit of a previously-unaudited spec)
**Spec**: `specs/keeper-state-machine/KeeperTaskAcquisition.tla` (178 LOC, bug-model paired) — task-acquisition control flow; pins "every task that is *claimed* must eventually be *finished* (no orphaned claim)"
**OCaml**: `keeper_unified_turn.ml` (`run_keeper_cycle` — has a "Spec navigation (OCaml -> TLA+)" reverse-citation block mapping `SubmitTask`/`AssignTask`/`EmptyQueueSleep`/`TurnComplete`/`TaskRejected`; the `AssignTask` → `"turn"` channel decision is the `observation.pending_mentions <> [] || observation.pending_board_events <> [] || observation.pending_scope_messages <> []` guard inside that function (literal in `keeper_world_observation.ml`)), `keeper_keepalive_signal.ml` (`post_submit_task ~meta ~task_id` with `[@@fsm_guard "meta.Keeper_types.current_task_id = Some task_id"]`, Cycle 44), `keeper_keepalive.ml` (`assign_keeper_task_from_directive ~agent_name ~task_id` — the operator `claim:<task_id>` directive path; writes `current_task_id`, calls `persist_directive_meta_update` then `post_submit_task` then `wakeup_keeper`), `keeper_world_observation.ml` (`Coord.read_backlog` → `observation.pending_*`)
**Verdict**: **Model accurate (FSM over `queue_depth` / `turn_state` / `tasks_claimed` / `tasks_completed`; `SubmitTask` ↑`queue_depth`, `AssignTask` ↓`queue_depth` + `turn:=running` + ↑`tasks_claimed`, `TurnComplete` `turn:=idle` + ↑`tasks_completed`, `EmptyQueueSleep`/`Done` stutter; bug-action `TaskRejected` ↓`queue_depth` + ↑`tasks_claimed` *without* `TurnComplete` → `NoTaskOrphan` / `QuiescentImpliesCompleted` / `InFlightMatchesFSM` violated). All cited symbols exist; the spec was already symbol-anchored (iter 64 N-2.a). Documentation imprecisions, all fixed comment-only: (1) the spec preamble cited `keeper_unified_turn.ml`'s `run_keeper_cycle` but not the *reverse-direction* "Spec navigation (OCaml -> TLA+)" block already living inside that function (full action mapping), nor `keeper_keepalive_signal.ml`'s `post_submit_task` `[@@fsm_guard "meta.Keeper_types.current_task_id = Some task_id"]` (Cycle 44) — the OCaml side carries a runtime-enforced mirror of one of this spec's actions and a reader couldn't find it from the spec. Same situation as iter 87 (OPB, `keeper_stale_watchdog.ml`) and iter 90 (KHB, `keeper_keepalive_signal.ml`'s other `[@@fsm_guard]`s). (2) there are *two* producer paths into the FSM and only one was described: the backlog producer (supervisor/autoresearch/board → `Coord.read_backlog` → next `world_observation.pending_*` → keeper claims via the `"turn"` channel = `AssignTask`) and the operator `claim:<task_id>` directive (`assign_keeper_task_from_directive` — bypasses the backlog, writes `current_task_id` directly, wakes the keeper; in spec terms a *fused `SubmitTask`+`AssignTask`* in one step, with `post_submit_task`'s `[@@fsm_guard]` enforcing the `current_task_id` write half). The spec's `SubmitTask` abstracts the common queue/turn-state/counter effect of both — but the preamble only mentioned "producers append tasks to the backlog", which doesn't cover the directive path.** Model body byte-identical; TLC re-verified (clean = no error, 55 states / 28 distinct; buggy = `SafetyInvariant` violated, 64 states / 37 distinct).

## Why this spec

iter 86–90 worked through never-first-entry-audited specs (KeeperTurnSlot, OperatorPauseBroadcast, KeeperWorkPipeline-banner, KeeperApprovalQueue, KeeperHeartbeat). KeeperTaskAcquisition (178 LOC, bug-model paired) is the last sizeable one in the never-audited tail — and the iter-90 audit flagged it specifically: `keeper_keepalive_signal.ml`'s `post_submit_task` `[@@fsm_guard]` references `KeeperTaskAcquisition.tla SubmitTask` (Cycle 44), the same annotated-correspondence pattern, so it was worth checking whether the spec picked it up. (It hadn't.)

## What was checked

| Spec element | Runtime | Status |
|---|---|---|
| `SubmitTask` (↑`queue_depth`) | (a) backlog producer → `Coord.read_backlog` → `observation.pending_*` (`keeper_world_observation.ml`); (b) operator `claim:<task_id>` directive → `assign_keeper_task_from_directive` (`keeper_keepalive.ml`) writes `current_task_id`, `post_submit_task` has `[@@fsm_guard "meta.Keeper_types.current_task_id = Some task_id"]` | ✓ — **spec preamble described only path (a)** |
| `AssignTask` (↓`queue_depth` + `turn:=running` + ↑`tasks_claimed`) | `run_keeper_cycle` (`keeper_unified_turn.ml`) — `observation.pending_mentions <> [] || observation.pending_board_events <> [] || observation.pending_scope_messages <> []` ⇒ `channel = "turn"`; the function's own "Spec navigation (OCaml -> TLA+)" block names this | ✓ — already a reverse-citation; spec didn't point at it |
| `TurnComplete` (`turn:=idle` + ↑`tasks_completed`) | `run_turn` body finishes, `run_keeper_cycle` returns `Ok updated_meta` / `Error sdk_error`; control falls to next observation cycle | ✓ |
| `EmptyQueueSleep` / `Done` | the `else` branch picks `"scheduled_autonomous"`; quiescent / saturation stutter | ✓ |
| `TaskRejected` (bug) — ↓`queue_depth` + ↑`tasks_claimed`, no `TurnComplete` | a `"turn"`-channel claim that early-returns without recording the turn outcome (`run_keeper_cycle` returns `Ok meta` on a non-executable phase / livelock-blocked turn) — the OCaml comment names exactly this ("Silent-drop regressions ... would violate the spec") | ✓ — well-formed |
| `queue_depth` ↔ backlog / `turn_state` ↔ keeper running-state / `tasks_claimed`/`tasks_completed` ↔ turn-outcome counters | abstract — no single OCaml field; the spec collapses the pipeline (as the preamble states) | ✓ |
| `.cfg` / `-buggy.cfg` | both present | ✓ |
| Bug-Model contract | clean = no error (55 states, 28 distinct); buggy = `SafetyInvariant` violated (64 states, 37 distinct) — re-verified this PR | ✓ — well-formed |
| line-ref drift | none in the spec — already symbol-anchored (iter 64 N-2.a). **But**: `keeper_unified_turn.ml`'s reverse-citation block says `Spec line 3 already cites this function: "[run_keeper_cycle] (line 1042+)"` — spec line 3 no longer has `(line 1042+)` (iter 64 N-2.a removed it). Stale *reverse*-direction self-reference in the OCaml comment; left as a follow-up (the OCaml comment is harmless, and editing it triggers a rebuild for a one-line cosmetic fix). | ✓ (spec) / ⚠ (OCaml comment, follow-up) |

## The drift (sub-class 2: code carries an annotated correspondence the spec didn't pick up; comment lags)

The OCaml side has *more* spec correspondence than the spec preamble advertised:

1. **Reverse-citation block in `run_keeper_cycle`.** `keeper_unified_turn.ml`'s `run_keeper_cycle` opens with a "Spec navigation (OCaml -> TLA+)" comment that names `specs/keeper-state-machine/KeeperTaskAcquisition.tla` (Cycle 8 / Tier B2, PR #11412) and maps every action: `SubmitTask` → external producers populate `observation.pending_*`; `AssignTask` → the `"turn"` channel decision (`observation.pending_mentions <> [] || observation.pending_board_events <> [] || observation.pending_scope_messages <> []`); `EmptyQueueSleep` → `"scheduled_autonomous"`; `TurnComplete` → `run_turn` returns a result; `TaskRejected` → bug action, claimed task dropped without a finish. The spec preamble cited `run_keeper_cycle` but not this block — so a reader of the spec couldn't find the authoritative OCaml action mapping. **Fix**: the preamble now says `run_keeper_cycle` carries the reverse-direction citation and names the `AssignTask` → `"turn"` guard.

2. **`post_submit_task` `[@@fsm_guard]`.** `keeper_keepalive_signal.ml` has `post_submit_task ~meta ~task_id` with `[@@fsm_guard "meta.Keeper_types.current_task_id = Some task_id"]` (Cycle 44), routed through `Keeper_fsm_guard_runtime.wrap_unit ~stage:"guard"` by the PPX — a runtime post-action check mirroring "after the directive attaches the `task_id`, the meta carries it". It's invoked from `keeper_keepalive.ml`'s `assign_keeper_task_from_directive` (the operator `claim:<task_id>` path), right after `persist_directive_meta_update`. The spec preamble didn't mention it. **Fix**: the preamble now names `post_submit_task`, its `[@@fsm_guard]`, and the call site.

3. **Two producer paths, one disclosed.** `SubmitTask` in the spec is just `queue_depth' = queue_depth + 1`. In the runtime there are *two* ways a task enters: (a) the backlog producer (supervisor/autoresearch/board → `Coord.read_backlog` → next `world_observation.pending_*`), and (b) the operator `claim:<task_id>` directive (`assign_keeper_task_from_directive` — bypasses the backlog, writes `current_task_id` directly, wakes the keeper). Path (b) is a *fused `SubmitTask`+`AssignTask`* in spec terms — it both introduces the task and attaches it to the keeper — and the `post_submit_task` `[@@fsm_guard]` enforces the `current_task_id` half. The spec's `SubmitTask` abstracts the common queue/turn-state/counter effect of both, but the preamble said only "producers append tasks to the backlog". **Fix**: a new "Producer paths" paragraph in the header spells out (a) vs (b) and why the spec doesn't track `current_task_id`; the `SubmitTask` action comment now points at it.

**Fix (comment-only)**: rewrote the preamble's first paragraph to name `run_keeper_cycle`'s reverse-citation block (with the `AssignTask` guard) + `post_submit_task`'s `[@@fsm_guard]` + the `assign_keeper_task_from_directive` call site; added a "Producer paths" paragraph distinguishing the backlog producer from the operator-directive path; updated the `SubmitTask` action comment to reference it; added an iter-91 re-verification date. Model body byte-identical.

## Sub-class placement & follow-up

- Class = **sub-class 2 (drift: code carries an annotated correspondence the spec preamble didn't point at; the spec's own action comment under-disclosed one of the two producer paths)** — the model is accurate; the spec docs just hadn't caught up to the OCaml side's reverse-citation + `[@@fsm_guard]` + the second producer path. Comment-only fix.
- **Follow-up owed (low-priority, no PR yet)**: `keeper_unified_turn.ml`'s `run_keeper_cycle` reverse-citation block still says `Spec line 3 already cites this function: "[run_keeper_cycle] (line 1042+)"` — spec line 3 no longer carries `(line 1042+)` (iter 64 N-2.a removed it). A one-line OCaml comment fix; deferred here to keep this PR spec-comment-only (editing OCaml triggers a full rebuild in CI for a cosmetic change). Bundle it with the next OCaml-touching keeper PR, or do it standalone.
- No follow-up PR owed for the spec. Comment-only — model body byte-identical; `specs/INDEX.md` regenerated (KeeperTaskAcquisition content-hash bump `e7143072d90f` → `a2b3be87b2c8`). The spec is in the `make -C specs check-clean` runner; CI re-checks it.
- This is *not* an RFC-gated subsystem — it's the task-acquisition *control-flow* spec; `keeper_unified_turn.ml` / `keeper_keepalive*.ml` / `keeper_world_observation.ml` are the cycle/keepalive/observation mechanism, not credential/identity action handlers. (Note: `assign_keeper_task_from_directive` is an operator-*directive* path but it's a task-claim, not a credential/identity action — operator credential handlers are `lib/operator/operator_control*`.) Not credential/keeper remote command/host_config, not repo_manager, not operator_control credential handlers, not keeper_sandbox/shell, not dashboard credential component, not .claude/hooks, not instructions/workflow. RFC-WAIVED.
- **Remaining never-first-entry-audited specs after this**: the never-audited tail is now essentially drained — KeeperEventQueue / KeeperCircuitBreaker had partial entries earlier; what remains are standing follow-ups: KeeperWorkPipeline model-bug fix (iter 88, `KNOWN_FAILURES` — clean spec violates invariant, exit 13), OperatorPauseBroadcast-buggy `Deadlock reached` vs intended `PauseLeadsToBroadcast` violation (iter 87), the `\.ml:[0-9]` zero-tolerance lint (still blocked — `KeeperPostTurnOrchestration.tla` has 9 line-range refs even after #14971 merged: the merged PR symbol-anchored the *mapping table* but left prose refs like `keeper_post_turn.ml:648-656` / `keeper_post_turn.ml:640-647`; needs a follow-up sweep before the lint can land), and the `keeper_unified_turn.ml` `(line 1042+)` reverse-ref above.
