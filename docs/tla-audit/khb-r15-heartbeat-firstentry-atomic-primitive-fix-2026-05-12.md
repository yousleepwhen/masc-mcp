# KHB R-15 — KeeperHeartbeat.tla: first-entry audit — model accurate; the spec named the wrong atomic primitive (`Atomic.exchange` vs `Atomic.compare_and_set wakeup true false`) and missed the fully-annotated OCaml correspondence in `keeper_keepalive_signal.ml` — sub-class 2

**Date**: 2026-05-12 · **Iteration**: 90 (`/loop` FSM/TLA+/OCaml drift hunt) · **Phase**: R (first-entry audit of a previously-unaudited spec)
**Spec**: `specs/keeper-state-machine/KeeperHeartbeat.tla` (144 LOC, bug-model paired) — heartbeat-loop wakeup control flow; pins "the heartbeat loop services every `FALSE → TRUE` edge of the wakeup atomic (no missed wakeups)"
**OCaml**: `keeper_keepalive_signal.ml` (`post_wakeup_signal` / `post_heartbeat_tick` with `[@@fsm_guard]` PPX; `interruptible_sleep` whose `Atomic.compare_and_set wakeup true false` is the `HeartbeatTick` action; `wakeup_keeper`), `keeper_heartbeat_loop.ml` (`run_heartbeat_loop` outer loop @1588; `run_smart_heartbeat_gate` dispatcher @1376; `Heartbeat_smart.Skip_idle`), `keeper_keepalive.ml` (`wakeup_keeper_by_agent_name`; `Atomic.set entry.fiber_wakeup true` @125; `include Keeper_heartbeat_loop`), `keeper_turn_fsm.mli`
**Verdict**: **Model accurate (FSM over `wakeup_signaled` / `turn_state` / `unserved_signals`; `WakeupSignal` ↑edge-counter on `FALSE→TRUE`, `HeartbeatTick` clears + ↓counter + `turn_state := running`, `TurnComplete`, `Done`; bug-action `MissedWakeup` clears the signal *without* starting a turn or decrementing → `NoMissedSignals` / `QuiescentImpliesServed` violated). All cited symbols exist; the spec was already symbol-anchored (iter 64 N-2.a). But: (1) the spec named the WRONG atomic primitive — the `HeartbeatTick` comment said "`[Atomic.exchange wakeup false]` returns true" whereas the runtime is `Atomic.compare_and_set wakeup true false` (CAS true→false, in `interruptible_sleep`); the CAS semantics matter (of two racing ticks only one wins). (2) the spec missed that the OCaml side carries a *fully-annotated correspondence* — `keeper_keepalive_signal.ml` has `post_wakeup_signal`/`post_heartbeat_tick` with `[@@fsm_guard]` PPX checks that mirror this spec's postconditions, and `interruptible_sleep`'s inline comment explicitly names `KeeperHeartbeat.tla HeartbeatTick action` and the `MissedWakeup` bug-action (the `Skip_idle` path). (3) `run_heartbeat_loop` is defined in `keeper_heartbeat_loop.ml` (re-exported via `Keeper_keepalive`'s `include`), not `keeper_keepalive.ml` directly; the consume site is `interruptible_sleep` in `keeper_keepalive_signal.ml`, the dispatcher is `run_smart_heartbeat_gate`.** Comment-only fix; model body byte-identical; TLC re-verified (clean = no error, 9 states / 4 distinct; buggy = `SafetyInvariant` violated).

## Why this spec

iter 86–89 worked through never-first-entry-audited specs (KeeperTurnSlot, OperatorPauseBroadcast, KeeperWorkPipeline-banner, KeeperApprovalQueue). KeeperHeartbeat (144 LOC, bug-model paired) is the next — and it's an atomic-CAS spec, exactly the kind where naming the wrong primitive in the citation is a real (semantic) drift.

## What was checked

| Spec element | Runtime | Status |
|---|---|---|
| `WakeupSignal` (↑`unserved_signals` on `FALSE→TRUE`) | `wakeup_keeper` / `Atomic.set entry.fiber_wakeup true` (`keeper_keepalive_signal.ml` / `keeper_keepalive.ml`@125); `post_wakeup_signal` has `[@@fsm_guard "Atomic.get wakeup = true"]` | ✓ |
| `HeartbeatTick` (clears + `turn_state := running` + ↓counter) | `interruptible_sleep`'s `Atomic.compare_and_set wakeup true false` succeeds → `Woken` → `run_smart_heartbeat_gate` dispatches a cycle; `post_heartbeat_tick` has `[@@fsm_guard "Atomic.get wakeup = false"]` (mirrors `wakeup_signaled' = FALSE`) | ✓ — **the spec said `Atomic.exchange wakeup false`; it's actually `Atomic.compare_and_set wakeup true false`** |
| `TurnComplete` / `Done` | turn returns to idle; quiescent stutter | ✓ |
| `MissedWakeup` (bug) — clear signal, no turn, no decrement | the `Heartbeat_smart.Skip_idle` branch consuming the CAS and then skipping the cycle (i.e. the `Woken`↔dispatch discriminator missing/wrong) — `interruptible_sleep`'s comment names exactly this risk | ✓ — well-formed (a follow-up `WakeupSignal` then pushes `unserved_signals` past the bound) |
| `wakeup_signaled` ↔ `entry.fiber_wakeup : bool Atomic.t` | `keeper_registry.ml` field; set in `keeper_keepalive.ml`, CAS-consumed in `keeper_keepalive_signal.ml` | ✓ |
| `.cfg` / `-buggy.cfg` | both present | ✓ |
| Bug-Model contract | clean = no error (9 states, 4 distinct); buggy = `SafetyInvariant` violated — re-verified this PR | ✓ — well-formed |
| line-ref drift | none — already symbol-anchored (iter 64 N-2.a) | ✓ (but the *file*/*primitive* in some citations was imprecise) |

## The drift (sub-class 2: code refactored, citation lags)

The wakeup-consume logic was refactored into `keeper_keepalive_signal.ml` (with `[@@fsm_guard]` PPX annotations explicitly mirroring this spec's actions/postconditions) and `keeper_heartbeat_loop.ml` (`run_smart_heartbeat_gate` dispatcher, `Heartbeat_smart.Skip_idle`). The spec's preamble and `HeartbeatTick` comment hadn't caught up:

1. **Wrong atomic primitive.** `HeartbeatTick` comment said "`[Atomic.exchange wakeup false]` returns true". The runtime is `Atomic.compare_and_set wakeup true false` (in `interruptible_sleep`). `exchange` always succeeds and returns the prior value; `compare_and_set` only succeeds if the value was `true` and returns a bool — so of two racing ticks only one wins the CAS (the other sees `FALSE`). The model is fine (single tick), but the citation named the wrong primitive — and the difference is load-bearing for the *race* the spec's counter is designed to detect (`interruptible_sleep`'s own comment: "a non-zero count means producers are racing the consumer, which is itself a bug class").
2. **Missed the annotated correspondence.** `keeper_keepalive_signal.ml`'s `post_wakeup_signal` (`[@@fsm_guard "Atomic.get wakeup = true"]`) ~ `WakeupSignal`, `post_heartbeat_tick` (`[@@fsm_guard "Atomic.get wakeup = false"]`) ~ `HeartbeatTick`, and `interruptible_sleep`'s inline comment ("Spec: KeeperHeartbeat.tla HeartbeatTick action — wakeup is consumed (TRUE -> FALSE) ... without the discriminator the [Skip_idle] branch would consume the CAS and then skip the cycle (the [MissedWakeup] bug-action)"). The spec preamble didn't point at any of this — so a reader couldn't find the OCaml side's runtime-enforced mirror of these postconditions. Same situation as iter 87 (OPB) where `keeper_stale_watchdog.ml` had a note the spec hadn't picked up.
3. **Imprecise file/function.** `run_heartbeat_loop` is `keeper_heartbeat_loop.ml`@1588 (re-exported via `Keeper_keepalive`'s `include Keeper_heartbeat_loop`), not `keeper_keepalive.ml` directly; the wakeup *consume* is `interruptible_sleep` in `keeper_keepalive_signal.ml`; the *dispatcher* is `run_smart_heartbeat_gate`@1376 in `keeper_heartbeat_loop.ml`.

**Fix (comment-only)**: rewrote the preamble's "Runtime entities modelled" block to name the correspondence anchors (`keeper_keepalive_signal.ml` with the `[@@fsm_guard]` PPX + `interruptible_sleep`; `keeper_heartbeat_loop.ml` with `run_heartbeat_loop` / `run_smart_heartbeat_gate` / `Heartbeat_smart.Skip_idle`; `keeper_keepalive.ml` re-exports + the `Atomic.set entry.fiber_wakeup true` setter) and corrected the atomic primitive (`Atomic.compare_and_set wakeup true false`, with a note on why the CAS semantics matter). Updated the `HeartbeatTick` and `MissedWakeup` comments to name `interruptible_sleep` / `run_smart_heartbeat_gate` / `Skip_idle` and the `[@@fsm_guard]` runtime mirror. Added an iter-90 re-verification date.

## Sub-class placement & follow-up

- Class = **sub-class 2 (drift: code refactored / wrong primitive named in the citation; comment lags)** — the OCaml side already has a fully-annotated, runtime-enforced correspondence (`[@@fsm_guard]` PPX); the spec preamble just hadn't been updated to point at it or to name the actual atomic primitive. Comment-only fix.
- No follow-up PR owed. Comment-only — model body byte-identical; `specs/INDEX.md` regenerated (KeeperHeartbeat content-hash bump `35344ff2f4be` → `127b595d597d`). The spec is in the `make -C specs check-clean` runner; CI re-checks it.
- This is *not* an RFC-gated subsystem — it's the heartbeat-loop wakeup control flow; `keeper_keepalive*.ml` / `keeper_heartbeat_loop.ml` are the heartbeat mechanism, not credential/identity action handlers. Not credential/keeper remote command/host_config, not repo_manager, not operator_control credential handlers, not keeper_sandbox/shell, not dashboard credential component, not .claude/hooks, not instructions/workflow. RFC-WAIVED.
- **Remaining never-first-entry-audited specs after this**: KeeperTaskAcquisition (178 LOC) is the main one left (note: `keeper_keepalive_signal.ml`'s `post_submit_task` `[@@fsm_guard]` annotation references `KeeperTaskAcquisition.tla SubmitTask` — same annotated-correspondence pattern, worth checking the spec picked it up). Plus the standing follow-ups: KeeperWorkPipeline model-bug fix (iter 88), OperatorPauseBroadcast-buggy deadlock-vs-property (iter 87), the `\.ml:[0-9]` zero-tolerance lint (waiting on #14971 merge).
