# KAQ R-14 — KeeperApprovalQueue.tla: first-entry audit — model accurate; it projects onto the `submit_and_await` variant (the `submit_pending` no-fiber path is out of scope, undisclosed) and the bug-action comment misattributed the `entry.resolver = None` case — sub-class 1

**Date**: 2026-05-12 · **Iteration**: 89 (`/loop` FSM/TLA+/OCaml drift hunt) · **Phase**: R (first-entry audit of a previously-unaudited spec)
**Spec**: `specs/keeper-state-machine/KeeperApprovalQueue.tla` (144 LOC, bug-model paired) — operator-approval-queue control flow; pins "every suspended fiber on `Eio.Promise.await` eventually wakes (resolved or expired)"
**OCaml**: `lib/keeper/keeper_approval_queue.ml` (1402 LOC) — `submit_and_await`@997, `submit_pending`@1090, `expire_stale`@1336, `resolve`-side@~879; `type pending_approval` with two parallel optional fields, `resolver : Agent_sdk.Hooks.approval_decision Eio.Promise.u option`@74 and `on_resolution : (Agent_sdk.Hooks.approval_decision -> unit) option`@75 (exactly one populated per entry)
**Verdict**: **Model accurate (FSM over `pending_count` / `suspended_fibers` / `submitted_total`; `Submit` ↑both, `Resolve`/`ExpireStale` ↓both, `Done` stutter; bug-action `ExpireStaleNoResolve` ↓`pending_count` only → `SafetyInvariant` violated). All cited symbols exist; the spec was already symbol-anchored (iter 64 N-2.a). Documentation imprecisions, all fixed comment-only: (1) the model projects onto the `submit_and_await` variant — `keeper_approval_queue.ml` has a *second* submit entry point, `submit_pending` (`~resolver:None` + an `on_resolution` callback, returns the id immediately — NO suspended fiber), whose failure mode is a dropped callback, not a permanently blocked fiber; that's a legitimate projection but wasn't disclosed in the mapping. (2) the entry was described as carrying its callback "as `entry.resolver`" — runtime has two parallel optional fields, `resolver` (the await variant) and `on_resolution` (the pending variant), exactly one populated; `expire_stale` handles both via two *independent* matches (`match entry.resolver with Some r -> resolve r ... | None -> ()`, then separately `match entry.on_resolution with Some f -> f ... | None -> ()`), so `resolver = None` does not imply a callback exists. (3) the `ExpireStaleNoResolve` body comment said the regression is "a future refactor moves the `pending` cleanup before the resolve" — but `expire_stale` is *already* removal-first (it removes stale entries from `pending` via `atomic_update`, captures each `(id, entry)` in `stale_ref`, then resolves/invokes from the capture); the real hazard is a refactor that loses or skips the captured entry/resolver in that post-removal step, not a reordering. (The original comment's separate "`entry.resolver = None` at expire time" clause was also a misattribution — a `None`-resolver entry is a `submit_pending` request with NO suspended fiber, so dropping it doesn't produce the modelled harm.)** Model body byte-identical; TLC re-verified (clean = no error, 23 states / 10 distinct; buggy = `SafetyInvariant` violated).

## Why this spec

iter 86 (KeeperTurnSlot) / iter 87 (OperatorPauseBroadcast) / iter 88 (KeeperWorkPipeline) worked through never-first-entry-audited specs; KeeperApprovalQueue (144 LOC, bug-model paired) is the next, and it's an Eio-promise-suspension spec — exactly the kind where a "which variant does the model cover" question matters.

## What was checked

| Spec element | Runtime | Status |
|---|---|---|
| `Submit` (↑`pending_count`, ↑`suspended_fibers`) | `submit_and_await`@997 — `Eio.Promise.create ()`@1017, registers entry with `~resolver:(Some resolver)`@1034, then the caller blocks on `Eio.Promise.await` | ✓ — models the `submit_and_await` variant |
| `Resolve` (↓both) | operator/rule-engine path — `resolve_with_policy`@1206 does `atomic_update pending (fun map -> SMap.remove id map …)`@1216-1221 *first* (capturing the entry into a `result` ref), then calls `resolve_entry`@1231 which runs `match entry.resolver with Some r -> Eio.Promise.resolve r decision \| None -> ()`@~881-882 and separately `match entry.on_resolution with Some f -> f decision \| None -> ()`@~884. Removal precedes the resolve calls by construction; no `Fun.protect` is involved. | ✓ |
| `ExpireStale` (↓both) | `expire_stale`@1336 — first `atomic_update pending` removes the stale entries and captures each `(id, entry)` in `stale_ref`@~1340-1352, then per captured entry: `match entry.resolver with Some r -> Eio.Promise.resolve r (Agent_sdk.Hooks.Reject reason) \| None -> ()`@1384-1386, then *separately* `match entry.on_resolution with Some f -> f (Agent_sdk.Hooks.Reject reason) \| None -> ()`@1387-1400 (two independent matches — `resolver = None` ≠ a callback exists) | ✓ — the model abstracts only the effect on the suspended-fiber population (the `Some resolver` branch) |
| `entry.resolver` / `entry.on_resolution` | `type pending_approval` fields `resolver : Agent_sdk.Hooks.approval_decision Eio.Promise.u option`@74 and `on_resolution : (Agent_sdk.Hooks.approval_decision -> unit) option`@75 — two parallel options, exactly one populated (`submit_and_await` → `resolver`; `submit_pending` → `on_resolution`) | ✓ |
| bug-action `ExpireStaleNoResolve` (↓`pending_count` only) | a future refactor that loses or skips the captured entry/resolver in `expire_stale`'s post-removal resolve step (the `Some resolver` branch dropped, or `stale_ref` mis-built) — the entry is gone from `pending`, the suspended fiber never resolved. (NOT a reordering of removal vs resolve: removal is already first by design via `atomic_update`/`stale_ref`.) | ✓ — well-formed (drives `suspended_fibers > pending_count` → `SuspensionMatchesPending` false; if the queue then drains, `QuiescentImpliesResolved` false) |
| `.cfg` / `-buggy.cfg` | both present | ✓ |
| Bug-Model contract | clean = no error (23 states, 10 distinct); buggy = `SafetyInvariant` violated — re-verified this PR | ✓ — well-formed |
| line-ref drift | none — already symbol-anchored (iter 64 N-2.a; the preamble already records the +245..+413 line drift from iter 63 #14919 that prompted the symbol switch) | ✓ |

## The doc imprecisions (sub-class 1: coverage gap — *disclosure-only*)

### (1) The `submit_and_await` vs `submit_pending` projection wasn't disclosed

`keeper_approval_queue.ml` has two submit entry points:
- `submit_and_await`@997 — creates an `Eio.Promise`, registers the entry with `~resolver:(Some resolver)`, then **blocks the caller on `Eio.Promise.await`**. The entry has a *suspended fiber*. This is the variant the spec models (`suspended_fibers` counts these).
- `submit_pending`@1090 — registers the entry with `~resolver:None` + `~on_resolution:(Some on_resolution)` (a callback), returns the id `: string` immediately. **No suspended fiber.** The decision is delivered later via the `on_resolution` callback. Its failure mode is a *dropped callback*, not a permanently blocked fiber — weaker, and out of scope here.

The original "Runtime entities modelled" block described `pending` as "Each entry carries a resolver that wakes the suspended fiber" — true only for the `submit_and_await` variant. **Fix**: the entity block now says each entry carries *either* a resolver (`submit_and_await`) *or* an `on_resolution` callback (`submit_pending`), and a new "Scope (which path is modelled)" paragraph spells out the projection and why the `submit_pending` path is out of scope. Same shape as the iter-86 KeeperTurnSlot alphabet-projection disclosure and the iter-78/79/80 `PhaseSet`-projection disclosures.

### (2) The entry's two-field structure, and the `ExpireStaleNoResolve` comment's removal-ordering claim

Two related runtime facts the comments got loose about:

- **`resolver` and `on_resolution` are two parallel optional fields**, not one. `type pending_approval` carries `resolver : ... Eio.Promise.u option`@74 *and* `on_resolution : (approval_decision -> unit) option`@75; `submit_and_await` populates `resolver` (Some) and leaves `on_resolution` None, `submit_pending` does the reverse. `expire_stale` handles both via **two *syntactically* independent matches** — `match entry.resolver with Some r -> Eio.Promise.resolve r (Reject ...) | None -> ()`@1384-1386, then *separately* `match entry.on_resolution with Some f -> f (Reject ...) | None -> ()`@1387-1400 — the two arms don't reference each other, so `expire_stale`'s control flow would still terminate cleanly for any of the four (Some/None × Some/None) combinations. Under the *current* producer invariant exactly one field is populated (so `resolver = None` does today imply `on_resolution = Some`); the independent-match shape is defensive against a future refactor that might relax that invariant. The "Runtime entities modelled" block had collapsed this to "recorded as `entry.resolver : ... Eio.Promise.u option`", and the scope note's `expire_stale` pseudo-code was `match entry.resolver with Some r -> ... | None -> f (Reject ...)`, both of which misdescribe the control flow. **Fix**: the entity block now lists both fields (exactly one populated per entry by construction), and the scope note shows the two independent matches with this caveat.

- **`expire_stale` is already removal-first.** The `ExpireStaleNoResolve` bug-action comment said the regression is "a future refactor moves the `pending` cleanup *before* the `Eio.Promise.resolve`" — but the runtime *already* does removal first: `atomic_update pending` removes the stale entries and captures each `(id, entry)` in `stale_ref`@~1340-1352, *then* the per-entry resolve/invoke runs off the capture. The modelled hazard is therefore not a reordering — it's a refactor that **loses or skips the captured entry/resolver** in that post-removal step (the `Some resolver` branch dropped, `stale_ref` mis-built), leaving the entry gone from `pending` with the resolve never run. **Fix**: rewrote the comment to describe the post-removal-loss hazard and note removal-first is already the design. (The original comment also had a separate "`entry.resolver = None` at expire time" clause — also a misattribution, since a `None`-resolver entry is a `submit_pending` request with no suspended fiber; the rewritten comment keeps an `NB:` noting that.)

## Sub-class placement & follow-up

- Class = **sub-class 1 (coverage gap), disclosure-only form** — the model is accurate for the variant it covers; only the documentation under-disclosed the projection, and the bug-action comment misattributed one runtime case. Comment-only fix.
- No follow-up PR owed. Comment-only — model body byte-identical; `specs/INDEX.md` regenerated (KeeperApprovalQueue content-hash chain `c54c2c2887ad` → `4167980e9bb3` (round-1 disclosure + bug-action correction) → `36bb593bbcf3` (round-2/3 Copilot follow-up — independent-match wording clarified, "Fun.protect-finally" misdescription replaced with the actual `atomic_update pending` / `resolve_with_policy` control flow)). The spec is in the `make -C specs check-clean` runner; CI re-checks it.
- (Optional, low-priority) a future spec could add a second projection covering the `submit_pending` / `on_resolution`-callback path and its dropped-callback failure mode — but that's a different (weaker) safety property and probably not worth a model; the disclosure is sufficient.
- This is *not* an RFC-gated subsystem — it's an operator-approval *queue control-flow* spec; `keeper_approval_queue.ml` is the queue mechanism, not a credential/identity action handler (the approval *decisions* are consumed elsewhere). Not credential/keeper remote command/host_config, not repo_manager, not operator_control credential handlers, not keeper_sandbox/shell, not dashboard credential component, not .claude/hooks, not instructions/workflow. RFC-WAIVED.
- **Remaining never-first-entry-audited specs after this**: a few small ones (KeeperEventQueue, KeeperHeartbeat, KeeperTaskAcquisition, KeeperCircuitBreaker — depending on how "audited" is counted; several had partial entries earlier). Plus the standing follow-ups: KeeperWorkPipeline model-bug fix (iter 88), OperatorPauseBroadcast-buggy deadlock-vs-property (iter 87), the `\.ml:[0-9]` zero-tolerance lint (waiting on #14971 + #14975 merge).
