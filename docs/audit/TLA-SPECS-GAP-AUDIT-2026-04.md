# TLA+ Specs Gap Audit — 2026-04-30

> Status: First-pass audit. Surveys Bug Model coverage and invariant strength across `specs/`.
> Author: Vincent (jeong-sik) with Agent-LLM-A
> Created: 2026-04-30
> Tracks: Q-P0-2 (TLA+ specs gap audit)
> Companion: `docs/audit/OAS-MASC-BOUNDARY-AUDIT-2026-04*.md` (Q-P0-3, MERGED)

---

## 1. Purpose

The repo has **84 manual `.tla` specs** across 17 domains (per `specs/INDEX.md`, auto-generated by `scripts/gen-tla-index.sh`). The AGENT-LLM-A.md `TLA+ Bug Model 패턴` rule states:

> **양쪽 cfg 모두 통과해야 spec이 유효하다**: clean이 pass하는데 buggy도 pass하면 invariant가 너무 약한 것. clean이 fail하면 모델이 잘못된 것.

This audit measures *how widely the rule is applied* and surfaces three classes of gap. As with the OAS audit chain (Phase 1→3), this is a **survey, not a design proposal** — fixes need domain expertise per spec and are deferred.

## 2. Method

```bash
# Domain-level coverage
for dir in specs/*/; do
  domain=$(basename "$dir")
  tla=$(find "$dir" -maxdepth 2 -name "*.tla" 2>/dev/null | wc -l | tr -d ' ')
  cfg=$(find "$dir" -maxdepth 2 -name "*.cfg" 2>/dev/null | wc -l | tr -d ' ')
  buggy=$(find "$dir" -maxdepth 2 -name "*buggy*" 2>/dev/null | wc -l | tr -d ' ')
  printf "%-30s tla=%3d cfg=%3d buggy=%2d\n" "$domain" "$tla" "$cfg" "$buggy"
done

# Tautology Bug Models (clean == buggy invariant set)
# Inspect specs/INDEX.md columns clean={...} buggy={...} for textual equality
```

`specs/INDEX.md` is auto-generated and lists per-cfg invariants. Comparing the `clean=` and `buggy=` columns directly surfaces tautology pairs without any new tooling.

## 3. Findings

### 3.1 Bug Model coverage by domain

| Domain | .tla | clean cfg | buggy cfg | Bug Model coverage |
|---|---|---|---|---|
| admission-queue | 1 | 1 | 1 | **100%** |
| auth | 1 | 1 | 1 | **100%** |
| autonomous | 2 | 2 | **0** | **0%** ❌ |
| boundary | 25 (18 unique) | 18 | 18 | **100%** |
| bug-models | 23 | 22 | 23 | mixed (split-pair, see §3.3) |
| checkpoint-trim | 1 | 1 | 1 | **100%** |
| closure | 1 | 1 | 1 | **100%** |
| keeper-state-machine | 46 | 27 | 23 | partial (composite specs) |
| keeper-turn-fsm | 1 | 1 | 1 | **100%** |
| masc-ecosystem | 1 | 1 | **0** | **0%** ❌ |
| multimodal | 4 (2 unique) | 2 | **0** | **0%** ❌ |
| resilience | 2 | 2 | **0** | **0%** ❌ |
| server-state | 1 | 1 | 1 | **100%** |
| shared | 1 | 1 | **0** | **0%** ❌ |
| social-state-cap | 1 | 1 | 1 | **100%** |
| state-product | 2 | 2 | 2 | **100%** |
| task-lifecycle | 1 | 1 | 1 | **100%** |

**5 domains, 8 specs at 0% Bug Model coverage**: `autonomous`, `masc-ecosystem`, `multimodal`, `resilience`, `shared`. Each has clean cfg with a real `Specification` declaration (verified via `rg "^SPECIFICATION" specs/<domain>/`), so the missing piece is the *paired buggy cfg + BugAction*, not the spec itself.

### 3.2 Tautology Bug Models (invariant set identical between clean and buggy)

Scanning `specs/INDEX.md` for `clean={X} buggy={X}` matches:

| File | Clean and buggy invariant set (verbatim from INDEX) |
|---|---|
| `admission-queue/AdmissionQueue.tla` | `{TypeOK, FdThresholdEnforced, CascadeNameCanonical, ReleaseCountBounded, ActiveCounterConsistent}` |
| `auth/AuthIdentityFSM.tla` | `{TypeOK, SafetyInvariant}` |
| `boundary/CascadeKeeperRecovery.tla` | `{SafetyInvariant}` |
| `boundary/CascadeStrategyStateful.tla` | `{TypeOK, StickyDomainBounded, StickyMembersAreCandidates, RRBounded, NoExpiredEntriesUsable, prop:StickyEventuallyEvicts}` |
| `boundary/KeeperContractViolated.tla` | `{Safety}` |
| `boundary/KeeperEmptyToolUniverse.tla` | `{Safety}` |
| `boundary/KeeperRecoveryOrchestration.tla` | `{SafetyInvariant}` |
| `boundary/KeeperStaleKilled.tla` | `{Safety}` |
| `boundary/KeeperTurnScheduler.tla` | `{SafetyInvariant}` |
| `boundary/KeeperTurnTerminal.tla` | `{Safety}` |
| `bug-models/AuthIdentityFSM.tla` | `{TypeOK, SafetyInvariant}` |
| `bug-models/DashboardCacheStampede.tla` | `{TypeOK, NoZombieSlot}` |
| `bug-models/DiscoveryCacheTTL.tla` | `{TypeOK, ConsistentRead}` |
| `bug-models/DispatchCoverage.tla` | `{TypeOK, DataFsmConsistent, PhaseConsistent, NeverStuckFailing}` |
| `bug-models/FileLockStarvation.tla` | `{TypeOK, FlockMutex, SingleMutexPerPath}` |
| `bug-models/HebbianLearning.tla` | `{WeightBounded NoLostUpdate}` |
| `bug-models/KeepalivePhaseConsistency.tla` | `{TypeOK, KeepalivePhaseConsistent, InFlightImpliesRunning}` |
| `bug-models/KeeperPhaseRace.tla` | `{TypeOK, CrashImpliesThreshold, RecordOnlyOnCrash, CounterBoundedByObservations}` |
| `bug-models/MemoryCompaction.tla` | `{ConstraintsPreserved NeverEmpty ResultBounded LongTermProtected RecentFloorRespected}` |
| `bug-models/SSEBroadcastBlock.tla` | `{TypeOK, NoPermanentBlock}` |
| `bug-models/SessionRegistryGhost.tla` | `{TypeOK, ConsistencyInvariant}` |

**21 specs** have textually identical clean and buggy invariant sets.

This **may** still be valid: the buggy cfg can override the `Spec`/`Init`/`Next` action (e.g. via a separate `SpecBuggy` definition that includes `BugAction`) while keeping the same invariants — that *is* the AGENT-LLM-A.md pattern. To confirm, the buggy `.cfg` file must reference a different `SPECIFICATION` than the clean `.cfg`.

```bash
# Sanity check (one example):
diff <(grep -E "^SPECIFICATION|^INVARIANT" specs/auth/AuthIdentityFSM.cfg) \
     <(grep -E "^SPECIFICATION|^INVARIANT" specs/auth/AuthIdentityFSM-buggy.cfg)
```

If the SPECIFICATION line is identical in both, the pair is a **true tautology** — buggy cfg adds no signal. Spot-checking a sample of the 21 candidates is the proper next step (deferred to a follow-up audit; needs domain-by-domain inspection).

The point is structural: `INDEX.md` reveals 21 candidate-tautology rows that warrant per-spec inspection, not that all 21 are wrong.

### 3.3 Split-pair Bug Models (separate `Bug.tla` file instead of unified spec)

`specs/bug-models/` contains the older split-pair pattern:

```
AmbiguousPartialCommit.tla     (clean only — buggy=0)
AmbiguousPartialCommitBug.tla  (buggy only — clean=0)
KeeperWorkPipelineBug.tla      (buggy only)
```

AGENT-LLM-A.md's pattern is **one spec file with `Spec` and `SpecBuggy` definitions** sharing the same module — `KeeperOASAdvanced.tla` is the canonical example (one file, `BugAction = CancelledAbsorbed`, invariant `CancelledNeverAbsorbed`). The split-pair style works, but:

- `INDEX.md` shows it as orphans (clean=0 or buggy=0 row) which the metric in §3.1 misclassifies.
- Two files mean the buggy variant can drift from the clean module's actual `Next` action without the divergence being visible.

The 3 split-pair specs in `bug-models/` predate the unified pattern and could be migrated; this is a hygiene improvement, not a correctness gap.

### 3.4 Empty `specs/states/` directory

`specs/states/` contains only `26-04-26-20-01-00.401/` — a TLC artifact directory (timestamped output). The `clean-artefacts` Makefile target (`scripts/cleanup-tlc-artifacts.sh`) should sweep it; if the artifact persists, the cleanup script likely missed this path or `states/` should be added to `.gitignore`. **Hygiene**, not a coverage gap.

```bash
$ ls specs/states/
26-04-26-20-01-00.401
$ ls specs/states/26-04-26-20-01-00.401/ | head -3
# (TLC trace files)
```

## 4. Severity ranking

| Class | Count | Severity | Rationale |
|---|---|---|---|
| 0% Bug Model coverage | 5 domains, 8 specs | **High** — missing the second auditor entirely | New domains have invariants but no falsification check. Adding a paired buggy cfg per spec would catch invariant-too-weak class. |
| Tautology candidate (invariant identical) | 21 specs | Medium — needs per-spec inspection | Some are valid (BugAction in SpecBuggy keeps invariants the same), some are weak. Cannot triage without reading each `.cfg` pair. |
| Split-pair style | 3 specs in bug-models/ | Low — hygiene | Pattern works; unification is a refactor that improves visibility. |
| `specs/states/` artifact | 1 directory | Low — hygiene | Cleanup script gap or `.gitignore` missing entry. |

## 5. Why fixes are deferred

Each Bug Model needs a domain-specific `BugAction` describing a real production bug:
- `autonomous/AutonomousLoop`: a plausible bug is "tick fires while phase=Idle" — but the clean spec already has `TickOnlyDuringRunning`. The BugAction must be subtle enough that the invariant catches it without rejecting valid behavior.
- `multimodal/MultimodalArtifact`: domain expertise needed — what failure mode in the artifact lifecycle is realistic to model?

Writing a buggy cfg without that domain knowledge produces *fake* Bug Models — an `assert false` in the BugAction would trip any invariant. The audit's role is to *flag the gap*, not to fabricate models.

This mirrors the OAS audit chain's discipline: Phase 2 refined Phase 1's verdict via taxonomy (C1–C4), it didn't refactor Layer C blindly.

## 6. Recommended ratchet (descriptive, not enforced)

Following the pattern from `scripts/oas-boundary-ratchet.sh` (PR #12117 MERGED), a TLA+ Bug Model coverage ratchet would track:

```bash
# Strict (descriptive only — would be enforced after baseline review)
domains_without_bug_model: count of domains where buggy_cfg_count == 0 AND tla_count > 0
# Floor: 5 (current). Goal: monotonically decrease.

# Tautology surface (descriptive)
candidate_tautology_specs: count of specs where INDEX.md shows clean={X} buggy={X} textually
# Floor: 21 (current). Inform per-spec triage.
```

The ratchet wouldn't gate CI immediately — both metrics need a sweep PR (or several) before the floor becomes a meaningful enforcement target. The OAS chain spent four phases moving from "needs sweep" to "ratchet enforced" exactly for this reason.

## 7. Suggested next steps (out of scope here)

1. **Per-spec triage of the 21 tautology candidates** (one-shot pass): `diff` the clean/buggy `.cfg` SPECIFICATION lines, classify into (a) valid `SpecBuggy` override, (b) true tautology — needs buggy invariant strengthening.
2. **Migrate 3 `bug-models/*Bug.tla` files** to unified-pair style: hygiene-only, not urgent.
3. **`specs/states/` cleanup**: investigate whether `cleanup-tlc-artifacts.sh` missed this path, or add `.gitignore` entry.
4. **Add buggy cfg + BugAction for 8 specs** in 5 domains: domain-by-domain, each as its own RFC + spec PR. Highest-value first (autonomous/AutonomousLoop is in the keeper-loop hot path; resilience specs interact with cascade orchestration).
5. **Add coverage ratchet** to `scripts/`: descriptive metric per §6, eventually promoted to enforced once §1–§4 baseline is solid.

## 8. References

- `specs/INDEX.md` — auto-generated, source for all per-spec data above
- `scripts/gen-tla-index.sh` — INDEX generator
- `specs/Makefile` — `check-clean`, `check-buggy`, KNOWN_FAILURES list
- `scripts/cleanup-tlc-artifacts.sh` — artifact sweeper
- `specs/keeper-state-machine/KeeperOASAdvanced.tla` — canonical unified Bug Model example
- `docs/audit/OAS-MASC-BOUNDARY-AUDIT-2026-04*.md` — sister audit chain (Q-P0-3, MERGED 4/4)
- AGENT-LLM-A.md `TLA+ Bug Model 패턴 (Mutation Testing for Specs)` section
- Memory: `feedback_self_confession_comments_must_be_measured` (don't trust self-confessing comments — same idea applied to specs: don't trust a buggy.cfg you didn't read)

*Audit date: 2026-04-30 / Phase 1 of N / docs-only, code change = 0 / opens Q-P0-2 chain*
