\* TLC Model Checking Configuration for KeeperStateMachine

CONSTANTS
    MaxRestarts = 3

SPECIFICATION Spec

\* State invariants (no temporal operators — checked at every state)
INVARIANTS
    TypeOK

\* Terminal states (Dead, Stopped, Zombie) deadlock by design — disable TLC deadlock check
CHECK_DEADLOCK
    FALSE

\* Temporal properties ([] and ~> — checked over all behaviors)
PROPERTIES
    DeadIsForever
    StoppedIsForever
    ZombieIsForever
    BudgetNeverRevives
    \* StopMonotonicity (removed: FiberStarted resets stop_requested)
    RestartCountMonotonic
    RunningRequiresFiber
    StoppedRequiresDrain
    DeadRequiresNoBudget
    OfflineRequiresLaunchPending
    ZombieRequiresTerminalFailureLatched
    FailingResolves
    CrashedRestartsEventually
    DrainingResolves
    CompactingResolves
    HandoffResolves
    CompactionClearsOverflow
    OverflowedResolves
