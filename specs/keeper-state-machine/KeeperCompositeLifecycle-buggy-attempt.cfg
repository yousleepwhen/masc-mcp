\* TLC Configuration — Buggy spec (RFC-0065 Phase 5.1.b: attempt without surface)
\* Expected outcome: ToolSurfaceFeedsAttempt MUST be violated.
\* If it holds, the invariant is too weak.

SPECIFICATION SpecBuggyAttempt

CONSTANTS
    MaxTurnTicks = 4

INVARIANTS
    ToolSurfaceFeedsAttempt

CHECK_DEADLOCK
    FALSE
