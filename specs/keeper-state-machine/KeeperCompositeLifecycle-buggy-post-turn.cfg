\* TLC Configuration — Buggy spec (RFC-0065 Phase 5.1.b: post-turn during attempt)
\* Expected outcome: PostTurnConsumesAttempt MUST be violated.
\* If it holds, the invariant is too weak.

SPECIFICATION SpecBuggyPostTurn

CONSTANTS
    MaxTurnTicks = 4

INVARIANTS
    PostTurnConsumesAttempt

CHECK_DEADLOCK
    FALSE
