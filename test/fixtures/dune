; Test-only sandbox fixtures: mock, executor functors.
; Relocated from lib/keeper/ (P1 thin-wrapper inline). These modules have
; zero lib/ consumers — they exist solely as test harness infrastructure.
; (wrapped false) preserves bare module names (Keeper_docker_client_mock etc.)
; so test code needs no import changes.

(library
 (name masc_sandbox_fixtures)
 (wrapped false)
 (libraries masc_test_deps))
